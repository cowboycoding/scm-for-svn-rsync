Todo
- Fix paths alle funktioner
- ? Tracking af produkt-paths/branches i ~/.scm/
- Add logging class
- Check svn stat ved release (alt skal være comitted før release)
- release-product skal auto-tagge så man kan reverte


Done:
- Move config til ~/.scmrc
